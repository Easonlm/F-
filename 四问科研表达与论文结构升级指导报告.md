# 四问科研表达与论文结构升级指导报告

**形成日期：2026-09-24。** 本文回应用户粘贴的《科研表达与论文结构升级》提纲，依据当前工作区的分问题报告、实际代码路径、结果表与[项目全局审计与优化路线](项目全局审计与优化路线.md)作可行性判断。**本轮只写指导文档；不重新拟合、不改变模型、不重算结果。**以下“下一轮”均为验证计划，不能写成已经完成的实验。

## 1. 总判断：方向可行，但“统一”必须写成有边界的连接

粘贴提纲最有价值的部分，是把四问从算法流水账改写为“数据表征 → 条件性能规律 → 算力决策 → 能力前沿对照”，并要求每个结果标明证据来源。现有代码和产物足以支撑这种**模块化科研叙事**。问题二经典 $N$–$D$ 律、问题三成本求解与解析条件、问题四 C8 解析等应保留；没有证据支持为叙事升级而更换模型。

但当前不能说四层已经被同一批真实实验“端到端验证”：问题一的 $Q_A$ 与问题二的 $Q_B$ 没有共同锚点；配比效应系数 $\lambda_p$ 在问题二是默认情景；问题四的 Loss–Benchmark 桥接组外 RMSE 约 11.86 分，严格开放权重前沿仅四季度，末季只有 2 个模型。因此建议论文主标题或核心表述采用**“证据约束下的数据配比、规模规律与资源配置：能力前沿的情景对照”**。可以使用“统一框架”，但须紧接着说明“统一的是问题结构、代码调用与条件决策流程；质量尺度和现实能力的统计识别仍有限”。

| 粘贴提纲的主张 | 当前判定 | 论文处理 |
|---|---|---|
| 四层科研框架、研究问题驱动的章节结构 | **现在可做** | 用一张链条图组织全文，每条箭头标明实际接口和证据等级。 |
| 22 指标质量代理、17 域配比、二阶 ALR、经典 Scaling Law 与 Q×N | **现在可做，附来源限定** | 分别说明 $Q_A$ 的构念效度尚缺人工验证、Q×N 仅在 B6/B7 半合成机制中获得支持。 |
| 问题三上下文成本临界长度与高预算增长指数 | **可作为条件命题** | 写清原始单位、约束不活跃条件、质量成本可忽略条件，再与现有数值结果核对。 |
| $Q$ 的三阶段“相图” | **部分可做，不能普遍化** | 写“模型条件下的资源配置区间”；对数质量成本下起始与饱和同点，未出现可辨的内部阶段。 |
| $\lambda_p$ 与 $Q_A\to Q_B$ 的“部分识别” | **需改术语** | 目前是“不能点识别 + 外加情景范围”，不能把人为范围称为数据给出的识别集。 |
| E0/E1/E2、情景包络、失败结果保留、贡献份额降级 | **现在可做** | 这些结果已计算或有证据，应进入正文主图与局限性段落。 |
| 1×/2×/3×/5× trust-region profile | **下一轮计算** | 目前正式输出是 E0/E1/E2；不能把尚未计算的 2×、5× 曲线写成结果。 |
| rolling p90/p95/Top-3 与最低样本规则 | **下一轮模型比较** | 当前季度 p90/p95/Top3 已有，但滚动窗口与 $n_{\min}$ 选型及时间外验证尚未完成。 |
| C3 进入核心时间动力学 | **需先验证可比性** | 当前 C3 主要用于来源和年度 p95 审计；不得说已驱动历史回归或未来预测。 |
| monotonic spline、分任务或多任务桥接 | **有条件的下一轮假设** | C6 只有 75 行、高可比 7 行；先看族外误差和折间波动，未改善就保留有界 logistic。 |

## 2. 四问当前最终版本与真实接口

当前主版本以[版本审计表](current_version_audit.csv)为准，不用历史 V1 图表替代 V2 结论。

| 层次 | 当前主结果 | 真正传给下游的内容 | 论文中应明示的断点 |
|---|---|---|---|
| 问题一 | 四组等权 $Q_A$；配比 V2 二阶 ALR Ridge | V2 配比模型嵌入 P2 artifact | $Q_A$ 被描述性读取，P2 质量项使用 B6 的 $Q_B$，没有数值标定的 $Q_A\to Q_B$。 |
| 问题二 | V2 Model C：经典 $N$–$D$ + $G(1-Q_B)^\kappa N^{-\eta_N}+\lambda_p\Delta_p(\mathbf p)$ | P3 V2 直接加载推荐 joblib | $\lambda_p=1$ 是默认情景，不是跨规模估计；B8 不符合统一质量方向。 |
| 问题三 | V2 成本优化、E0/E1/E2、联合参数 bootstrap、稳健情景 | P4 调用 V2 求解方法并在未来预算上重算 | P4 未直接读取 P3 最优结果表；E1 的 3× 边界是人为敏感性设定。 |
| 问题四 | 严格开放权重前沿、C6 桥接、结构回归与预测情景 | 能力对照是最终展示层 | C3 不在主动力学拟合中；Loss→Benchmark 的组外误差大，不能作精确能力换算。 |

**建议的总图标注：** $X_{22}\to Q_A$〔内部一致性、扩展一致性；人工效度缺〕；$\mathbf p\to\Delta_p$〔1M 测试支持、跨规模偏移〕；$(N,D,Q_B,\lambda_p\Delta_p)\to L$〔B1 与 B6/B7 支持、B2/B8 限制〕；$L+C\to (N^*,D^*,Q_B^*,\mathbf p^*)$〔给定模型与成本的数学最优〕；$L\to S$〔C6 有误差的统计桥接〕；历史前沿 $\leftrightarrow$ 机制情景〔短历史对照，不是外部因果验证〕。图中把 $Q_A\dashrightarrow Q_B$ 画成虚线，标“无共同锚点”。

## 3. 论文要讲的研究问题和贡献

建议开篇问题是：**在质量量尺、来源损失口径和远期外推均不完全可识别时，怎样把给定附件中可信的规律转换成条件资源决策，并诚实地与现实能力数据对照？** 这个问题比“提出新算法全面预测未来大模型”更符合项目实际。

贡献建议收敛为三条，逐条给出证据边界：

1. **可复核的数据与配比表征。** 22 指标形成可解释的 $Q_A$ 工程代理；对配比使用符合 simplex 约束的 ALR 表示，二阶 V2 在既定 1M 测试和嵌套 CV 中优于 V1。它不证明 $Q_A$ 等于真实训练效用，也不证明领域置换的因果效果。
2. **保留简单基线的条件 Scaling Law 与资源决策。** B1 的经典 $N$–$D$ 式同族拟合很强，B6/B7 中 Q×N 优于加性 Q；P3 把训练、质量与上下文成本放入同一约束并报告支持域。B2 和 B8 表明这个条件模型不能当跨来源普适律。
3. **从 Loss 到能力的可审计情景对照。** P4 清洗开放模型、解析 C8 逐任务结果、估计有误差的 C6 桥接，并让数据驱动与前三问机制预测并列。规模/技术分解和 12/24 月分数是探索性情景，不是已校准的未来预测。

“结构创新”可以用于**组织与诊断方法**，不能声称新发现了现实中的统一因果机制。建议摘要最后一句写“本框架同时给出可迁移规律的适用范围与不可识别接口”，不要写“完整打通质量到能力的因果链”。

## 4. 四问逐章改写：保留什么、如何写、何处止步

### 4.1 问题一：从“评分算法”写成“质量代理与配比预测的双任务”

**应保留的证据。** A1–A3 总计 272,505 条质量记录经 22 指标处理；四组等权、固定 A1 标尺、冲突规则与去重扩展检查已经完成。A1 与 A2/A3 有重合，去重后 arxiv/github 同域均值差约 -0.0031/-0.0009，支持*扩展一致性*。配比 V2 嵌套 OOF RMSE 0.3951→0.3340、1M A6/A7 RMSE 0.3415→0.2702；跨 60M/1B 绝对 Loss 的 RMSE 为 1.596/2.702，说明排序和数值迁移要分开讨论。

**建议段落结构。** 先定义质量的操作性构念、预处理方向与权重选择；再给出内部复算、冲突敏感性、去重扩展；最后单列配比建模与 1M/跨规模测试。$Q_A$ 和 $\mathbf p$ 在当前数据中没有被联合随机操纵，不能写成“质量和配比的独立因果分解”。领域置换是给定模型、给定邻域的局部预测差。

**建议表述。** “$Q_A$ 是依据 22 项观测信号构造的质量代理；现有证据支持实现一致性和部分分布稳定性，尚缺人工盲评及独立训练效用验证。” 人工结果表目前为 0 行；不要把已设计的 140 条双评审模板写成已完成验证。A12–A15 是估算标签，不能作为真实 10B/70B 外推实测。

### 4.2 问题二：从“拟合多个函数”写成“简单基线、质量作用及失败边界”

主式保持当前 Model C：

$$
L=E+AN^{-\alpha}+BD^{-\beta}+G(1-Q_B)^\kappa N^{-\eta_N}+\lambda_p\Delta_p(\mathbf p).
$$

**先写为何保留简单项。** B1 经典式同族留 $N$ 组 RMSE $1.51\times10^{-4}$；更复杂的 broken-$N$、interaction 和稳健残差没有稳定改善跨来源表现。极低误差来自同族、近确定性生成结构，不代表现实跨族精度。B2 raw RMSE 1.243，须与 B1 数字并列。20% 目标来源标签的校准属于有监督适配，不能称 B1 零样本泛化。

**再写质量项的证据。** 在 B6 半合成数据上，按 $N$ 留一的 RMSE 从加性 0.08144 降到 Q×N 的 0.05235；B7 新点从 0.05952 降到 0.04213。B8 仍约 1.34，且 B6/B8 有 160 个相同 $(N,D,Q)$ 对应不同 Loss 的冲突点、质量方向反转。正文可把它写成“单一 $L(N,D,Q)$ 形式在这两套机制间不可同时满足”的数据诊断，不能声称已经识别了 B8 的真实生成机制。

**最后写接口边界。** $Q_B$ 是 B6 量尺；$Q_A$ 到 $Q_B$ 无实证换算。$\lambda_p=1$ 仅定义配比项的参考幅度，不能说已从联合实验估计；$Q_{mix}=g(\mathbf p)$ 又由固定域分数和配比决定，现有数据无法独立分开 $Q$ 与 $\mathbf p$ 的效应。所谓“广义 Scaling Law”应写成**条件组合模型**，并列出上述识别假设。

### 4.3 问题三：从“求几个最优点”写成“条件命题、约束活动与外推分层”

**可直接写入正文的解析命题 1。** 以原始 $N,D$ 单位，$C_{train}=6ND$、$C_{attn}=\eta NDL_{ctx}$；当前参数对应

$$
\frac{C_{attn}}{C_{train}}=\frac{L_{ctx}}{30000},\qquad L_{ctx}^{crit}=30000.
$$

它只比较训练项与注意力项；不表示在 30000 上下文处总成本或最优解必然突变。报告中的十亿单位代码写法是 $C_{train}=6\times10^{18}N_BD_B$、$C_{attn}=2\times10^{14}N_BD_BL_{ctx}$，比值一致。

**可写的解析命题 2，必须加条件。** 在 $Q_B=1$、配比固定、质量成本相对于总预算可忽略且 $N,D$ 上界不活跃时，$C\propto ND$ 导出 $N^*\propto C^{\beta/(\alpha+\beta)}$、$D^*\propto C^{\alpha/(\alpha+\beta)}$。当前参数的理论弹性为 0.451522/0.548478；现有 $10^{24}$ 自由 E2 数值局部弹性为 0.444625/0.561340。这个对照属于**条件数学一致性**。E1 在 $10^{24}$ 时两维触及 3× 上限、预算只用 22.4%，不能把上述无界渐近式用于 E1 最优解。

**把“相图”改成资源配置区间图。** 对指数/幂质量成本，现有数值有 $Q=Q_0$、内部 $Q_0<Q<1$、$Q=1$ 的起止点；对数成本的起始与饱和同为约 $2.0036\times10^{18}$ FLOPs（4096 上下文），中间阶段没有可辨预算跨度。故正文应画实际约束活动图，并注明成本形式改变区间形态；“结构转移”是优化解的活动约束变化，不是物理相变，也不是外部观测到的历史阶段。

**E0/E1/E2 应作主图。** $10^{24}$ FLOPs 下三层 Loss 分别约 2.0934/1.9794/1.9160，预算利用率约 2.6%/22.4%/100%。E1 的 3× 只是情景半径。高预算稳健方案在 36 个情景均碰共同上界而得零后悔，应解释为边界角点的数学结果，不可称决策风险已消失。联合 bootstrap 只传播 B1/B6 已建模参数及成本情景，未覆盖跨来源质量机制不确定性。

### 4.4 问题四：从“未来分数预测”写成“历史样本、桥接误差与情景对照”

**数据事实优先。** 严格实体匹配接受 46/4576 行；开放权重六项完整样本 45 个，其中 pretrained 32、chat/finetuned 10、其他 3。严格样本四个季度的末季只有 2 个模型；W1 首末 p95 变化 -2.80，而较宽的 C2 继承匹配口径为 +10.01。两者方向不同，正是需要展示的样本选择敏感性，不能任取一条“更好看”的线。

**C3 的真实用途。** 当前 `problem4/data_pipeline.py` 用 C3 做来源/年份 p95 产物审计，主历史回归和预测不以 C3 为动力学训练序列。正文应如实写“用于历史来源对照和时间信息核查”；在未审查 C3 与 C1/C4 的实体及指标可比性前，不应为了满足“必用”而强行塞入预测回归。若赛题对 C3 要求更强，见第 6 节下一轮验证路线。

**桥接与分解要降级。** C6 有 75 条，其中高可比 7 条；有界单调 logistic 的家族组外 RMSE 11.86 分。C8 解析 1860 个模型目录与 74,344 个逐任务分数，同榜单六项均值 Pearson 约 0.952，这支持解析的一致性，不能算独立外部能力验证。规模/时间项模型隐含变化 +6.84 分，拆为算力 +2.15 和残余时间项 +4.69；实际严格前沿 -2.80。31.5%/68.5% 只能列在**模型内反事实分解**或敏感性小节。“技术”项应改称“控制算力后的剩余时间项”，其中混合架构、数据、对齐、选择和未测因素，不是纯技术进步的因果效应。

**未来结果分四类展示。** 统计参数 bootstrap、模型残差带、外生算力/技术情景包络、P3 的 E0/E1/E2 外推范围各有不同含义，不能合称“95% 未来置信区间”。中度放缓下数据驱动 12/24 月为 43.37/57.51，24 月条件 95% 范围 [22.14,99.15]，且没有 12/24 月同期限回测；E1 机制路径两期均约 20.94，来自 $N,D$ 上界钳制和有误差的 C6 桥接。正文同时呈现 E0/E1/E2 与数据驱动路径，不拿 20.94 当能力停滞预言。预测原点是 2025-01-31，目标日 2026-01-31/2027-01-31；论文明确这些是相对于原点的模型情景，不是当前日期的实时预测。

## 5. 粘贴提纲中四处必须修正的术语

| 术语 | 当前可用的严格写法 | 不能写成 |
|---|---|---|
| **Partial identification** | “$\lambda_p$ 与 $Q_A\to Q_B$ 不能点识别；在明确外加假设下报告敏感性集合。”只有范围端点由数据和识别假设共同推导时，才称正式识别集。 | 把 $\lambda_p\in[0,1.5]$ 或三条单调映射当作数据估计出的部分识别区间；更不能称其为置信区间。 |
| **Trust region / E1** | “E1 是 3× 上界的外推敏感性约束”；E0/E1/E2 是已计算的三级结果。 | 说 3× 是经经验估计的安全范围，或把 1×/2×/3×/5× profile 说成已完成。 |
| **Structural regime transition** | “在给定 Loss、成本和上下界下，$Q$ 活跃约束随预算改变。” | 三阶段对所有成本形式必然存在，或现实产业发生物理相变。 |
| **Capability frontier / scenario envelope** | “有限样本定义下的观察前沿”与“模型及外生设定下的未来路径”分列。 | 把模型 +6.84 当观测增长，把条件区间和情景上下界合并为经校准的未来 95% 预测区间。 |

五个英文关键词不必机械全部放进摘要。**Generalized Scaling Law**、**Evidence-Constrained Optimization** 可用，但要附适用域；**Structural Regime Transition** 只用于条件优化图；**Partial Identification** 在当前证据下宜写“未点识别与情景敏感性”；**Capability Frontier Forecasting** 宜写“能力前沿情景对照”。

## 6. 以后要优化的地方：先有触发条件，再改模型

以下清单继承[优化假设表](optimization_hypotheses.csv)，是后续计算或新增证据任务，**本轮均未实施**。优先级按影响链条的大小排，而非预期 RMSE 降幅。

| 下一轮任务 | 当前触发证据 | 最小验证路线与接受标准 | 失败后的论文写法 |
|---|---|---|---|
| **A. 锁定四问接口与版本** | V1/V2 JSON 并存；P4 方法重算而非读最优表；现有验证器检查较浅。 | 核对 P1 V2 配比系数、P2 Model C 参数、P3 成本/$Q_0$/上下文和 P4 实际输入的数值及哈希；全部相等才能引用“当前最终链”。 | 若不等，先定位错接并重算受影响结论；这是最可能升级为 P0 的项目。 |
| **B. 完成 $Q_A$ 人工盲评，并区分 $Q_A/Q_B$** | 人工结果 0 行；两量尺没有共享实验。 | 冻结既有 140 文本抽样和双评审规则，报告评审一致性及与 $Q_A$ 的相关方向；$Q_A\to Q_B$ 另需共同锚点才估映射。 | 若盲评不支持，只称工程代理；即使盲评通过，也不能自动得到 $Q_B$ 映射。 |
| **C. 重审 P4 观察前沿与贡献份额** | 末季 $n=2$；严格 -2.80 与宽口径 +10.01 反向；模型隐含 +6.84。 | 预设最低样本规则、滚动窗口、p90/p95/Top-3，并比较短期时间外误差和口径方向；方向与组外表现稳定后再讨论份额。 | 若合理定义仍反向，放弃“规模/技术占比”的主结论，只报告可识别范围和模型内分解。 |
| **D. 核对 $\lambda_p$ 的跨规模幅度** | 默认 1；旧 V1 的 1M/60M/1B 斜率不能直接代替 P1 V2。 | 冻结 V2 配比预测，预先划分跨规模校准/留出，比较常数与低维尺度函数的留出误差及排序；独立改善才估参数。 | 维持 $\lambda_p$ 多情景，不报精确收益。 |
| **E. 拆解 P4 桥接和机制差异** | C6 组外 RMSE 11.86；E1 20.94 与数据路径差 22.43/36.58。 | 先比较现有有界 logistic 与少参数单调替代的家族组外误差；再拆桥接、时间趋势、E0/E1/E2 边界贡献。必须超过折间波动且可比性方向一致才换桥接。 | 保留最简单有界模型、宽误差与不一致结论。 |

**第二梯队。** E1 的 1×/2×/3×/5×/free 阈值剖面有明确动机：$10^{24}$ 时 3× 解只使用 22.4% 预算；若最优配置随半径剧变，就只报外推条件下的范围。C3 的实质利用应先核查日期、实体和分数口径；若无法与严格样本对齐，保留来源审计用途即可，不因“必须使用”而制造伪纵向数据。分任务桥接或共享参数多任务桥接应在 C6 样本足够、残差按任务有可重复结构且家族组外改善时才实施。Q 冲突惩罚、三阶配比、神经网络 Scaling Law、复杂 24 月时间模型目前没有升级门槛所需的证据。

## 7. 论文立即可以重塑的叙事与版式

这部分只改论证与呈现，不依赖新拟合。建议四问各章都按同一顺序写：**研究问题 → 数据与来源口径 → 模型与识别假设 → 估计/求解 → 组内和组外验证 → 失败诊断 → 不确定性/适用域 → 本问能支持的结论**。不要让优化器、算法名称先于研究问题出现。

| 位置 | 现在应完成的写作动作 | 支持材料 |
|---|---|---|
| 摘要与引言 | 用“条件决策 + 证据边界”概括贡献；一句话说明 $Q_A/Q_B$、$\lambda_p$、C6 的弱连接。 | 本文第 2–5 节。 |
| 数据章节 | 一张附件证据谱系表：直接观测、半合成、估算标签、混合桥接；列出可比性与版本。 | 各问数据审计、[证据等级表](evidence_level_audit.csv)。 |
| 问题一 | 把 $Q_A$ 的内部稳定与真实效度分开；配比小规模性能与跨规模绝对偏移并列。 | `problem1/problem1_report.md`、P1 V2 比较表。 |
| 问题二 | 先解释为何经典律应保留，再展示 Q×N 的半合成证据、B2/B8 失败与 $\lambda_p$ 假设。 | `problem2_v2/problem2_v2_report.md`。 |
| 问题三 | 给两个带条件的解析命题、一幅真实活动约束图、一张 E0/E1/E2 主表；解释零后悔边界。 | `problem3_v2/problem3_v2_report.md` 及已有结构点表。 |
| 问题四 | 把观察前沿、模型隐含变化、数据驱动未来、机制未来画成不同系列；在图上显示末季 $n=2$ 与桥接误差。 | `problem4/problem4_report.md`、`problem4/outputs/tables/`。 |
| 讨论与结论 | 用“成立范围—失败范围—下一步所需证据”代替只报最优点；B2、B8、桥接高误差和远期宽区间必须保留。 | [可疑好结果](suspiciously_good_results.md)及全局审计。 |

**公开论文证据层级可用三级，但每一级再注明数据性质。** ① *直接数据支持*：如 A4/A5 内嵌套 CV 与既定 1M 测试、B1 同族规律、C8 解析一致性；② *模型条件支持*：如 B6/B7 半合成 Q×N、给定成本的 P3 优化、C6 有误差桥接；③ *情景探索*：如 $\lambda_p$、E2 高预算、P4 24 月和模型内贡献百分比。这里“直接数据支持”并不等于因果或真实世界泛化。不要把[内部审计表](evidence_level_audit.csv)的 A/B/C/D 当作论文里未经解释的评分。

**可直接借用的结论句式：**

> 在同源 B1 数据上，经典规模律提供了足够简单且拟合稳定的基线；B2 的大误差表明训练来源和 Loss 口径的变化不能由该基线自动吸收。

> 在 B6/B7 半合成机制内，质量惩罚随规模变化的表达优于加性质量项；B8 的同点冲突限定了该机制的跨源适用范围。

> 在给定成本和 Model C 的条件下，质量投资及 $N$–$D$ 配置存在约束活动转移；高预算绝对解对外推上界敏感。

> 严格开放权重样本的历史长度不足以识别稳健的规模/技术份额，12/24 月路径因此报告为情景包络，并保留与机制预测的不一致。

## 8. 完稿前的核查清单与本轮结论

1. 每张图表标明主版本、数据性质、样本量、验证方式和是否越过训练支持域；B1 极低 RMSE、P3 高预算零后悔须给解释。
2. 所有 $Q$ 符号加下标或在首次出现时定义量尺；所有 $\lambda_p$、质量成本、E1 3×、未来增长倍率都标为假设。
3. P4 的能力前沿必须区分实际观测 -2.80、模型隐含 +6.84 与宽口径 +10.01；31.5%/68.5% 不进摘要主结论。
4. 12/24 月展示条件区间的同时写“对应期限无回测”；情景包络、参数区间和桥接误差不混用统计名称。
5. 最终合稿前核对赛题要求的文献、附件、图表和 AI 使用披露；现有研究代码与分问报告还不是一份可直接提交的四问整合论文。

**结论。** 粘贴 prompt 的科研表达方向值得采用，最有效的本轮工作是重构论文论证、补齐命题的适用条件、把弱接口和失败结果放到读者看得见的位置。现有模型已足够形成一篇有明确边界的四问论文；下一轮实验应优先修复最上游的质量量尺与版本接口、最下游的稀疏前沿和高误差桥接。升级的目标是让结论更可信，而非让每个 RMSE 更低。
