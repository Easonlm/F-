# F题初步思路与任务大纲

> 这是供团队讨论和执行的工作草案。模型形式、参数范围、特征方向和最终结论必须经数据检验后确定；下列公式是候选方案，不应直接复制为论文结论。

## 1. 建议的总体策略

先建一个**简单、可检验的基线**，再逐步加入质量、领域配比及跨数据源桥接。每个新增模块都要通过独立验证证明它比前一版有用。核心结果保留连续 Loss 和 Benchmark 分数，不将其随意离散为类别。全程保存数据来源等级、处理规则、训练/检验划分和随机种子。

建议团队按两条线协同：数据与模型线完成 A/B/C 的清洗、拟合、验证；论证与写作线同步维护假设、单位、图表目录和局限性。所有组员应共同复核最终公式和主结论。

## 2. 分阶段执行表

| 阶段 | 推荐任务 | 应交付的中间成果 | 通过条件 |
|---|---|---|---|
| 0. 数据审计 | 清点 A1–A18、B1–B12、C1–C10；核对行数、表头、缺失、重复、单位、`index` 唯一性；为文件标记真实性等级 | `数据字典`、审计表、读取脚本 | 核心文件可读，数值范围和配对关系合理 |
| 1. 质量评分 | 流式读取 A1–A3 全量；压缩列表型信号；统一方向；稳健缩放；构造样本级与域级 $Q$ | 22 指标规则表、七域质量分及区间、抽样与扩展对照 | 更换合理权重/缩放时主要域排名不过度翻转；异常值可解释 |
| 2. 冲突处理 | 定义高教育价值与高广告量等有明确语义的冲突；估计域别冲突率；比较不同聚合规则 | 冲突类型、发生率、处理前后 $Q$ 变化 | A1 与 A2/A3 对共有域可对照，不预设冲突率为零 |
| 3. 配比模型 | A4/A5 训练；A6–A11 分尺度测试；A12–A15 只做外推稳健性；分析主效应和少量有依据的交互 | 17 域边际/组合影响、13 域 Loss 预测、检验误差 | 报告测试集 MAE/RMSE 与简单基线的比较，解释跨尺度差异 |
| 4. 标度律 | B1 拟合经典模型；B2 或 B3 与 B4/B5 验证；由问题一结果引入 $Q,\mathbf p$；B6–B8 仅辅助识别质量项 | 基线和广义模型、参数区间、残差图、弹性 | 参数物理方向合理，跨族误差及不确定性明确 |
| 5. 预算优化 | 以 $\log N,\log D,Q$ 及可行配比为决策；按 C7 选择上下文场景；多启动求解并校验约束 | 三档以上预算配置、成本分解、结构转移图 | 每个解满足预算与配比约束；质量成本形式改变时给出差异 |
| 6. 能力预测 | C1/C3/C4/C8 清洗和匹配；C6 分层桥接；规模与时间效应分解；滚动回测与区间预测 | 六维能力趋势、贡献占比、12/24 月前沿区间 | 开源/日期/模型类型口径明确，历史回测覆盖率可报告 |
| 7. 论文整合 | 统一符号、量纲、引用；复跑结果；做敏感性与局限性总结 | 最终论文、代码、复现实验说明 | 表图可追溯到脚本；所有数值与正文一致 |

## 3. 问题一：质量、冲突和配比

### 3.1 质量指标到 $Q$

首先逐字段核验取值、缺失和方向。列表型信号用有语义的统计量压缩，如中位数、有效项占比或分位数；`ad_en` 等负向指标反向处理。对于长度、句子数、平均词长等可能存在“过高也不好”的指标，不宜直接假定越大越好，应依据分布和内容抽查设合理区间或钟形效用，再统一为“处理后越高越好”。缩放参数仅用基准数据拟合，再应用于扩展集，避免不同文件分别缩放使 $Q$ 失去可比性。

一个透明的起点是 $Q_j=\sum_k w_k z_{jk}$，其中 $z_{jk}\in[0,1]$ 是方向统一后的信号，$w_k\ge0$ 且 $\sum w_k=1$。先按“教育性/可读性/洁净度/文本结构”等维度分组，组内和组间等权；再以专家调整权重、鲁棒中位数聚合或主成分模型作敏感性对照。域级 $Q$ 可用样本分数的中位数或截尾均值，并用 bootstrap 给区间。A1 中有 `content`，可抽样做人工文本核验；A2/A3 没有正文，只能比较信号分布和域级分数。

### 3.2 冲突的可操作定义

先确定少数语义明确的指标对或指标组，比如“高教育价值但高广告量”“高流畅度但低洁净度”。按分位数定义“显著高/低”；问题一实作统一使用 A1 全体记录的 75%/25% 分位阈值，以便跨域比较，并对阈值做敏感性分析。逐域报告冲突率、典型文本和分数影响。消解可比较两种规则：对高广告等硬性风险设惩罚项，或按冲突程度降低评分可信度。规则是否改善结果，应看人工核验和跨文件稳定性。

### 3.3 17 域配比到 13 域 Loss

配比向量位于单纯形上，不能同时放入 17 个比例和截距作普通线性回归。可先用“删去一个参考域 + 正则化线性模型”做可解释基线；再尝试能处理非线性及交互的模型。若用对数比变换，需明确零值处理并验证结果稳定。13 个验证域宜分别预测，随后根据事先声明的权重形成综合 Loss；权重不能由测试集反推。交互只保留有数据支持且方向可解释的少量域对。A16 的 `direct/near_direct/inferred` 映射分别赋予不同可信度；不宜把 7 域质量分强行当成 17 域的实测质量。

## 4. 问题二：广义标度律与替代关系

先拟合题目给出的基线 $L_0(N,D)=E+AN^{-\alpha}+BD^{-\beta}$，用非线性最小二乘或稳健损失估计，并施加 $A,B,\alpha,\beta>0$ 约束。因 B1 是同一模型的多个检查点，验证时按**模型运行或模型族分组**，避免随机拆行造成信息泄漏。对 B2/B3、B4/B5 的口径差异单独报告。

候选扩展可写为 $L=L_0(N,D)+h_Q(N,D,Q)+h_p(\mathbf p)+h_{Qp}(Q,\mathbf p)$。要求附加项有清楚的参考点和量纲，例如用 $Q_0$、参考配比 $\mathbf p_0$ 使附加项在参考配置下为零。也可把 $Q$ 纳入有效数据量 $D_{eff}=D\,f(Q,\mathbf p)$，但必须通过验证证明这种耦合优于加性模型。由于 A 和 B 独立采集，$Q$ 和 $\mathbf p$ 的作用可能只能分阶段估计；应明确哪些参数是直接识别、哪些靠半合成数据或外推假设。

边际效用用偏导数 $\partial L/\partial N$、$\partial L/\partial D$、$\partial L/\partial Q$；弹性用 $\varepsilon_x=(x/L)(\partial L/\partial x)$，预期改善方向为负。固定其他变量时，“提高 $Q$ 与增大 $N$ 等效”的局部条件为 $dL=0$，即 $dN/dQ=-(\partial L/\partial Q)/(\partial L/\partial N)$；必须说明参考点和单位。不同领域替代或互补可通过配比受约束的交互项或沿单纯形的置换方向计算，不能仅凭原始相关系数判断。

## 5. 问题三：优化与结构性转移

目标为最小化问题二给出的预测 Loss，约束为

$$6ND+D[g(Q)-g(Q_0)]_++\eta NDL_{ctx}\le C,\qquad p_i\ge0,\quad\sum_i p_i=1.$$

其中 $\eta=2\times10^{-4}$，$L_{ctx}$ 外生；$L_{ctx}^{crit}=30{,}000$。从 C7 的 `max_position_embeddings` 提取真实可行场景，并在临界值两侧取有数据支持的代表值。设定 $N,D,Q$ 的合理上下界，特别防止优化器逃向训练数据覆盖范围以外的极小 $N$ 或极大 $D$。先固定问题一得到的稳健配比方案优化 $N,D,Q$，再在可靠的配比预测区域内联合优化 $\mathbf p$，比较所得改善是否大于模型误差。

至少使用 $10^{19}$、$10^{22}$、$10^{24}$ FLOPs 三档，三种题给 $g(Q)$ 均运行。以多启动约束优化或低维网格加局部求解交叉核验，逐解核算成本三项及约束余量。结构性转移可预先定义为：随 $\log C$ 增加，最优 $N/D$ 比、$Q$、配比或三项成本份额出现稳定拐点，或某变量从边界解转为内点解。用分段回归或变点法估计转移预算，并对参数不确定性重算转移位置。

## 6. 问题四：能力分解和预测

六维分数先统一方向与 0–1 尺度，明确均等权还是任务权重，并同时展示各维结果，避免综合分掩盖差异。通过 C1/C3 的模型名、类型和日期与 C4 匹配；模糊匹配须保留匹配质量分数并抽样复核。按开源权重、许可证、pretrained 与 chat/finetuned 及日期口径建立筛选表。C8 按模型目录选最新可解析 JSON，完成至少一个逐任务聚合，例如每个模型的任务内均值、任务间离散度及与排行榜六维汇总的偏差。

在高可比的 C6 桥接样本上拟合单调的 Loss–Benchmark 映射；中可比样本用分层效应或仅作敏感性检验。再建立分层时间模型，例如能力 $S_{it}=f(\log N_i,\log D_i,\log C_i,\text{模型类型})+\tau(t)+u_{family}+\epsilon_{it}$。按历史时期的观测能力变化分解“规模投入变化”和“时间/技术残差”两部分；名称应准确称为**条件统计分解**，除非有充分识别策略，不能宣称严格因果效应。预测 12 或 24 个月开源前沿时，在“算力增速延续/放缓”等情景下给出中位数及预测区间，用滚动时间回测评估覆盖率和误差。前沿可定义为固定月/季度的高分位数或可重复的 top-k 均值，避免单个异常模型支配结果。

## 7. 优先处理的风险与推荐图表

| 风险 | 推荐处理 |
|---|---|
| 质量指标方向和尺度不一致 | 维护逐字段规则表；抽查文本；对方向不确定指标做两套情景 |
| A/B/C 数据独立，参数识别弱 | 分模块估计，保留桥接误差与情景区间，避免伪造逐模型联合观测 |
| 半合成数据“验证”循环 | 主模型优先真实数据；半合成用于机制/灵敏度；外部真实点单独测试 |
| 配比闭合效应与跨尺度偏移 | 用参考域或对数比处理，分尺度报告误差，加尺度校准项 |
| 预测依赖时间口径 | 固定日期、开源和模型类型筛选；做滚动回测与口径敏感性 |

推荐的核心图表：质量指标分布和方向表、七域 $Q$ 及区间、冲突矩阵、配比边际效应、标度律拟合与残差、不同预算的 $N/D/Q$ 和成本份额、结构转移图、六维 Benchmark 历史前沿、规模/技术贡献分解、预测扇形区间。

## 8. 下一步最具体的任务

1. 编写数据审计脚本，生成所有必用文件的行数、表头、缺失比例、数值范围、来源等级和可连接键。
2. 流式读取 A1–A3，完成 22 指标规则表；先给出七域质量分的一个透明基线版本。
3. 用 A4/A5 建立配比模型最简基线，同时锁定 A6–A11 为外部检验；暂不使用 A12–A15 调参。
4. 用 B1 拟合经典标度律并按模型分组验证，随后再讨论怎样引入 $Q$ 和 $\mathbf p$。
5. 在完成前三项的真实数据核验后，再确定问题三优化变量范围和问题四预测口径。
