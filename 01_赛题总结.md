# F题赛题总结：算力约束下提升大语言模型能力的资源配置建模

> 本文件用于团队理解题目和核对要求，不是已完成的建模结论。依据为根目录赛题 Markdown、《数据说明2.pdf》及对若干实际数据表头和行数的核验。数据说明中的来源、结构与划分仍以磁盘上的实际文件复核。

## 1. 核心问题与主线

在有限计算预算下，如何联合选择模型参数量 $N$、训练 token 数 $D$、数据质量 $Q$ 和 17 个训练领域的配比 $\mathbf p$，使验证集交叉熵损失尽量低，并解释这种配置如何转化为下游能力的提升。

四问依次衔接：**质量与配比评价 → 广义标度律 → 预算约束优化 → 历史能力分解与前沿预测**。前三问主要以 Loss 为结果变量，第四问以六维 Benchmark 为能力指标，需建立可检验的 Loss–Benchmark 桥接关系。Loss 越低越好；Benchmark 得分通常越高越好，两者不可直接等同。

## 2. 四问任务与最低交付

| 问题 | 必须回答的内容 | 主要输出 | 关键检验 |
|---|---|---|---|
| 一：数据质量与领域配比 | 统一 22 个质量指标方向，构造可解释的 $Q$；定义并处理指标冲突；刻画 17 域配比对 13 个验证域 Loss 的影响 | 样本及域级质量分、冲突分析、配比效应模型 | 全量 A1–A3；A6–A11 检验；A12–A15 外推稳健性 |
| 二：广义标度律 | 用 B1 拟合 $N,D$ 基线，整合问题一的 $Q,\mathbf p$；估计边际效用、弹性和质量与规模的替代条件 | $L(N,D,Q,\mathbf p)$、参数与不确定性、验证结果 | B2 或 B3；B4、B5；B6 或 B7/B8；B9、B10 |
| 三：资源优化 | 在 $C_{train}+C_Q+C_{attn}\le C$ 下优化 $N,D,Q,\mathbf p$；比较质量成本函数；识别预算变化时的结构性转移 | 多档预算的最优配置、成本分解、敏感性和转移判据 | 至少三个数量级预算；$L_{ctx}$ 按 C7 可行值设置 |
| 四：技术演进与预测 | 定义综合能力及开源口径，拆分规模扩张与非规模技术进步的贡献；预测未来 12 或 24 个月开源模型能力前沿 | 历史贡献占比、前沿预测区间、Loss–Benchmark 桥接误差 | C1 或 C2、C3、C4、C8 必用；C6 或 C5 桥接 |

## 3. 数据目录速览

全部附件位于 `real_attachments/`。说明书估计压缩数据约 0.55 GB；实际文件优先于说明书与 `source_manifest.json`。无需 GPU，但大文件宜流式读取。

| 附件 | 主要内容 | 已核对的实际结构 | 使用提醒 |
|---|---|---|---|
| A：`A_data_value/` | SlimPajama 质量信号、RegMix 配方与 Loss、领域映射 | A4 训练配比 512 行，17 个配比列；A5 对应 512 行，13 个 Loss 列；A16 映射 17 行 | A1–A3 的 22 个质量指标需全量处理；A2/A3 无文本正文，只有信号与 ID；配方表和 Loss 表用 `index` 连接 |
| B：`B_scaling_laws/` | Pythia 轨迹、跨族点、文献点、半合成 N–D–Q 实验 | B1 1,176 行，含 `N_params_B`、`D_tokens_B`、`val_loss`；B6 360 行 | `N_params_B`、`D_tokens_B` 均以十亿为单位；B2、B6–B8 是半合成；B10 为估算 Loss，不可作为独立真实观测 |
| C：`C_efficiency_evolution/` | 排行榜、Epoch 元数据、Loss–Benchmark 桥接、架构、逐任务 JSON | C1 4,576 行、六维得分；C4 3,523 行；C6 75 行；C7 45 行 | C8 需至少完成一项逐任务聚合；C6 按 `Loss_Comparability` 分层；C3 时间与评测口径混合 |

质量信号中的 22 个指标含 14 个标量和 8 个列表型指标；实际字段名包括 `rps_lines_ending_with_terminal_punctution_mark`，代码须以实际键名为准。质量数据的 7 域与 RegMix 的 17 域并不一一对应；A16 是参考映射，并非真实测得的跨域效果。配方的 17 个训练域只有 13 个对应验证 Loss 域，未出现的 4 域不能简单删掉，因为它们仍占训练配比。

## 4. 问题三给定的成本与必须报告的临界点

题目给出

$$C_{train}=6ND,\qquad C_Q=D[g(Q)-g(Q_0)]_+,\qquad C_{attn}=\eta NDL_{ctx},\quad \eta=2\times10^{-4}.$$

总成本不超过预算 $C$。$N$ 是参数个数、$D$ 是 token 个数，不能直接把以十亿为单位的 CSV 列代入。$L_{ctx}$ 是**外生场景变量**，其可行取值需从 C7 获得。由 $C_{attn}=C_{train}$ 得解析临界值 $L_{ctx}^{crit}=6/\eta=30{,}000$。题目建议预算 $10^{19}$、$10^{22}$、$10^{24}$ FLOPs，并要求至少三个不同数量级；质量成本函数至少比较题目附录 B 给出的指数、幂函数、对数渐进型设定。

## 5. 论文必须说清的边界

1. **真实性等级。** 真实观测、半合成、插值、模型外推和估算须分层汇报，不能把它们混成一个“实测训练集”。A12–A15 中 Loss 是外推值，配比是训练配比子集；它们适合做稳健性分析，不构成独立实测验证。
2. **可识别性。** A、B、C 分别采集，不能假定同时观测到每个模型的 $N,D,Q,\mathbf p$ 和六维 Benchmark。跨数据源拼接依赖显式假设、域映射和桥接误差，结论须限定适用范围。
3. **跨尺度。** 1M 配方实验得到的绝对 Loss 关系不能未经校准直接用在 1B、10B、70B。须区分“配比相对排序是否稳定”和“绝对 Loss 是否可预测”。
4. **能力度量与时间。** 第四问需明确开源权重及许可证筛选、pretrained 与 chat/finetuned 分层、日期口径、缺失处理和不同年代评测的可比性。
5. **AI 使用规范。** 赛题规定 AI 可辅助概念理解、资料整理和代码调试；核心建模、推导与论证须由团队核验、理解并完成。正式论文需披露 AI 使用环节，理论与结论引用可核验数据或正式文献。

## 6. 完成标准

最终论文应包含：变量与单位表、数据利用清单、四问模型和推导、训练/验证划分、误差与不确定性、成本和情景敏感性、关键图表、局限性、参考文献及 AI 使用披露。任何具体数值结论都应能从保存的数据处理脚本与结果表复现。
