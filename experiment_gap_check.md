# 实验缺口核查

结论：**本轮论文第二版无需重新运行建模实验**。选定链已有与每个核心主张相匹配的验证表，当前主要缺口是证据性质和论文表述，而非通过补跑相同脚本可以消除的问题。仅需只读核对冻结表、图像与文稿数字，避免改变原始输出。

| 核查点 | 已有结果 | 结论与处理 |
|---|---|---|
| 问题一模型选择 | 嵌套 OOF、A6/A7、60M、1B、bootstrap、13 域结果、候选模型 | 证据足够。1B 有 5 域退步；主文必须报告。既定测试集为回顾性比较。 |
| 质量真值 | 四组等权、CRITIC、PCA、熵权、权重/阈值敏感性；140 条盲评表未评分 | 不能补造人工验证。需要真正人工盲评或受控训练才可验证训练价值；不属于本轮必须实验。 |
| 问题二质量交互 | B1 留 N、B6 留 N、B7 新点、B8 冲突、Q×N×D bootstrap | 证据足以选 Q×N 作条件模型。跨来源原始 Loss 偏移和 Q_A/Q_B 无锚点必须写清。 |
| 问题三求解 | D 消元与数值根比对、KKT、三成本、联合 bootstrap、E0/E1/E2、minimax | 数值求解充分。高预算可信度取决于外推上界，重复优化不能提高外部证据等级。 |
| 问题四桥接与前沿 | C6 家族组外 CV、线性/logistic/isotonic、严格口径 45 模型、前沿敏感性 | 证据足够进行条件展示。末季 n=2，12/24 月直接回测缺失，历史分解与观测不一致。 |
| 全链接口 | `selected_lineage.json` 记录 P1 嵌入、P2/P3 artifact 与 P4 摘要 hash | 可追溯。论文应给出接口类型和误差传播，不把间接桥接称联合验证。 |

## 真正需要新数据的后续问题

1. 人工质量标签或同配比异质量训练，以检验 `Q_A` 的真实训练效用。
2. A/B 共同锚点，以估计 `Q_A → Q_B` 数值映射。
3. 多规模独立配比再训练，以识别 λ_p(N) 而非设定情景。
4. 同口径 Loss 与六基准能力的跨家族成对样本，提高桥接精度。
5. 目标日期之后的同口径前沿记录，才能做 12/24 月滚动回测。

这些属于新数据获取或实验设计，不是本轮应以现有数据“补跑”出来的量。第二版不以未完成项目充当已做检验。
