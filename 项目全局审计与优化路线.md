# 项目全局审计与优化路线

审计日期：2026-09-24。审计目标是判定现有四问模型的证据等级、版本接口和可证伪的最小修改路线；**没有改写或重跑原模型**。此文是内部方法审计，不是赛事官方评分，也不把“核验脚本通过”解释为现实泛化成立。

**总判定。** 四问在代码层面已形成可追踪链条，P1 的配比 V2、P2 的经典 $N$–$D$ 基线与 Q×N 条件模型、P3 的成本求解与证据分层都值得保留。最弱处是 P1 的质量代理 $Q_A$ 到 P2 半合成质量 $Q_B$ 缺共同锚点，以及 P4 的严格历史前沿只有四季度、末季 (n=2)，使规模/技术份额和 12/24 月精确预测不能作为主结论。没有发现已证实的算力单位错误、公式实现错误或逐行训练/测试泄漏；发现了**非封存开发集、旧接口并存、验证器检查过浅和跨问弱连接**。如按当前工作区直接参赛，尚缺四问整合论文与正式 AI 使用披露的提交核查。

本次审计形成的机器可读表：[`current_version_audit.csv`](current_version_audit.csv)、[`optimization_hypotheses.csv`](optimization_hypotheses.csv)、[`evidence_level_audit.csv`](evidence_level_audit.csv)、[`keep_modify_drop_matrix.csv`](keep_modify_drop_matrix.csv)、[`optimization_priority_matrix.csv`](optimization_priority_matrix.csv)。专项附件：[`project_dependency_and_risk_map.md`](project_dependency_and_risk_map.md)、[`suspiciously_good_results.md`](suspiciously_good_results.md)、[`evidence_literature_review.md`](evidence_literature_review.md)。

## 1 项目总体结构与审计范围

```text
问题一：A1-A3 → 质量代理 QA；A4-A15 → 17域配比预测器 p→13域Loss
             ↓                         ↓
问题二：B1经典N-D + B6半合成QB×N + P1配比模型 → Model C Loss(N,D,QB,p)
             ↓
问题三：Ctrain + CQ + Cattn ≤ C → N,D,QB,p 条件最优与E0/E1/E2
             ↓
问题四：C6 Loss→Benchmark；C1/C3/C4/C8 历史前沿与情景预测
```

审计了根目录赛题原文、《数据说明2.pdf》13 页、四问 README/报告/摘要/完整详解/版本比较/假设/核验，六个问题目录下 **40 个主 Python 文件（合计约 5851 行）** 的语法和关键数据路径，168 个项目 CSV 中与结论有关的结果表、28 个 JSON 中的接口和核验、6 个 joblib 模型，以及 77 张 `outputs/figures` PNG 的文件完整性并抽查三张关键图。还读取了问题一现有 Word 正文的章节与结论。AST 解析 40/40、joblib 加载 6/6、图片解码 77/77；这些是文件完整性检查，**不是全部数值实验重跑**。问题一 Word 只覆盖问题一，不是四问合稿。根目录没有统一 README/统一运行入口。

《数据说明》明确 A12–A15 与 B10 是估算/外推，B2 与 B6–B8 是半合成，C6 是混合可比性桥接；本审计严格按此分级。赛题需要的必用数据大体均已进入代码，但 C3 主要是来源审计，不是 P4 主动力学拟合的输入。

## 2 当前最终版本与历史版本

详见 [`current_version_audit.csv`](current_version_audit.csv)。口径如下：

| 问题 | 当前可用于论文的主版本 | 历史或辅助版本 | 版本事实 |
|---|---|---|---|
| P1 | 四组等权 $Q_A$ 沿用 V1；配比模型为 V2 二阶 ALR Ridge | V1 ALR Ridge、原质量惩罚候选 | V2 配比 `mixture_v2.joblib` 是下游嵌入对象；人工盲评结果仍为空 |
| P2 | V2 **Model C**：classic $N$–$D$ + $G(1-Q_B)^\kappa N^{-\eta_N}$ + $\lambda_p\Delta_p$ | V1 Model A 加性 Q；Model B 为需目标源标签的校准应用 | `recommended_model.joblib` 选 `classic/quality_model`，默认 `lambda_p=1` 是情景 |
| P3 | V2 证据约束版：E0/E1/E2、联合 bootstrap、minimax 后悔 | V1 基础求解器和旧 P4 JSON | V2 读取 P2 V2 的 joblib，但也依赖 P3 V1 表；E1 的 3× 是人为敏感性边界 |
| P4 | 目前只有一个主实现 `run_problem4.py` | `time_only`、`compute_only`、`nd_time` 为对照 | 使用 P3 V2 方法重算机制预测；最终报告同时保留明确的失败诊断 |

旧 `problem2/outputs/tables/problem2_to_problem3.json` 与 `problem3/outputs/tables/problem3_to_problem4.json` 仍存在。当前 P4 的执行路径没有读取它们；版本并存仍增加人工取错和未来改动误连的风险。`problem2_v2/outputs/tables/problem2_to_problem3_v2.json` 实际由 `problem3/load_inputs.py` 写出，目录名不能单独证明产物归属。

## 3 统一审计尺度

`Strong` = 代码、结果和适用范围有一致证据；`Acceptable` = 完成题意但附条件；`Weak` = 关键证据不足；`Missing` = 要求或外部验证尚未形成。P3 的“拟合”指作为优化输入的预测模型，不能把求解器的最优性当成实测拟合能力。

| 维度 | P1 | P2 | P3 | P4 |
|---|---|---|---|---|
| 问题覆盖度 | Acceptable | Acceptable | Strong | Acceptable |
| 数据使用完整性 | Strong | Strong | Strong | Strong；C3以描述性使用 |
| 模型合理性 | Acceptable | Acceptable | Strong（条件于输入） | Weak（前沿样本稀疏） |
| 参数可识别性 | Weak（Q权重语义） | Weak（QA→QB、λp） | Acceptable（给定模型后） | Weak（时间与算力分离） |
| 训练拟合能力 | Acceptable | Strong（B1/B6同源） | Acceptable（输入继承） | Acceptable（32样本内） |
| 泛化能力 | Weak（跨规模绝对值） | Weak（B2/B8） | Weak（继承上游） | Weak（时间外不足） |
| 外推能力 | Weak | Weak | Weak（1e24） | Weak（12/24月） |
| 稳健性 | Acceptable | Acceptable | Strong（多成本/边界） | Weak（口径反向） |
| 不确定性 | Acceptable（条件bootstrap） | Acceptable（同源bootstrap） | Acceptable（模型+情景） | Weak（期限不可校准） |
| 数值稳定性 | Strong | Strong | Strong | Acceptable |
| 可解释性 | Acceptable | Acceptable | Acceptable | Weak（时间残差） |
| 因果边界 | Strong（报告自限） | Strong（报告自限） | Strong（条件决策） | Acceptable（报告自限） |
| 数据泄漏风险 | Acceptable（既定测试曾被查看） | Acceptable（外部集用于开发比较） | Strong（无新拟合泄漏） | Acceptable（家族CV但短历史） |
| 工程复现性 | Acceptable | Acceptable | Acceptable（依赖V1） | Acceptable（依赖预产物） |
| 问间接口一致性 | Weak（质量链） | Strong（模型传P3） | Acceptable（方法传P4） | Acceptable（机制重算） |
| 论文可写性 | Strong | Acceptable | Acceptable | Weak（必须降级份额和点预测） |

## 4 总体优点：先保留已经有效的工作

1. **数据可信度分级准确。** 代码与报告区分真实、半合成、估算、混合桥接；P1 检验配方按 `index` 配对，P2 保留 B2/B8 失败，P3 单列 E0/E1/E2，P4 桥接分高/中可比。参见各问数据审计表和 `problem4/outputs/tables/data_audit_c.csv`。
2. **复杂模型没有自动替换简单基线。** P1 V2 二阶模型有嵌套 CV 和 A6/A7 同向改善；P2 broken-N、交互项、鲁棒残差并未稳定超越经典式，故保留 `classic`。这符合“理论或数据依据 + 验证改善 + 复杂度合理”的升级门槛。
3. **数值优化审计较完整。** P3 三项 FLOPs 单位正确，解析 $D$ 消元与数值根最大相对差 $3.26\times10^{-16}$，预算绑定误差 $2.22\times10^{-16}$，KKT 最大相对差约 $4.24\times10^{-6}$；这支持*给定模型及约束*的求解质量。
4. **负面结果没有被隐藏。** P2 B2 跨源失败、B8 同点冲突，P4 历史分解与实测方向不一致，P4 的 12/24 月回测缺席，均在报告中披露。应保留这些适用边界，而非为较低 RMSE 删除数据。

## 5 总体风险与证据类型

以下 `D1…D8` 对应数据、可识别性、模型形式、验证、外推、接口、不确定性、解释缺陷；`E1…E5` 对应项目结果、数据诊断、数学推导、正式文献、代码检查。

| 风险 | 类别 | Evidence | 实际后果 |
|---|---|---|---|
| $Q_A\to Q_B$ 无共同锚点 | D1/D2 | **E3/E5：** `Qmix` 由固定域 Q 与 p 确定；P2 V2 的 Q 项只由 B6 拟合，`QA_QB_scenarios.csv` 三情景均 `empirical_anchor=False` | P3 质量投资不能解释为对 P1 质量分的已验证投资 |
| $\lambda_p(N)$ 未联合识别 | D2/D5 | **E1/E5：** V1 中心化斜率 1M/60M/1B 为 0.900/0.658/0.285；最终 artifact 固定 `lambda_p=1` | P3/P4 的配比收益是规模情景，不是测得效益 |
| B1 同源精确但跨源偏移 | D1/D4 | **E1：** B1 留 N 组 RMSE 0.000151，B2 raw 1.243 | P2 的小 CI 不可传播为跨模型族现实 CI |
| 1e24 的真实支持不足 | D5 | **E1：** P3 E0/E1/E2 Loss 2.0934/1.9794/1.9160；E0/E1 分别只用预算 2.6%/22.4% | 最优建议由外推边界主导 |
| P4 前沿口径不稳定 | D1/D4/D8 | **E1：** W1 末季 (n=2) 时实测变化 -2.80；C2 继承宽口径首末 +10.01 | 规模/技术百分比不能升主结论 |
| P4 机制桥接误差大 | D1/D4/D7 | **E1：** C6 家族组外 RMSE 11.864；E1 机制 20.94 与中度放缓数据驱动 43.37/57.51 相距大 | 不能用机制点分数解释实际能力前沿 |

## 6 问题一专项审计

### Strengths

- **Q 预处理完整。** `quality_pipeline.py` 遍历 A1 51,230、A2 17,523、A3 203,752 条，对 22 项做列表/logit 压缩、负向补变换、五项“适中最好”梯形效用，使用 A1 固定分位点标尺。`quality_proxy_validation.md` 独立复算转换，22 项最大绝对差为 0。四个分组降低 DSIR 高相关项的重复权重。此为实现正确性，不能推导语义真值。
- **扩展集去重检验真实完成。** A1 与 A2/A3 分别重合 1,419/10,000 个 ID；去重后 arxiv/github 平均 (Q) 差 -0.0031/-0.0009，冲突率差亦单列。见 `quality_extension_dedup.csv`。这比直接把扩展表称独立验证稳妥。
- **冲突规则透明。** 预设 75/25 分位矛盾及连续强度，A1 冲突率 7.83%；改 80/20 与 70/30 后为 3.93% 与 13.62%。惩罚系数敏感性已做，主 Q 不强行使用惩罚。
- **配比 V2 升级有证据。** 嵌套 OOF RMSE 从 0.3951 降至 0.3340；A6/A7 1M RMSE 从 0.3415 降至 0.2702，平均 Spearman 从 0.9069 升至 0.9452。`problem1_v2.py` 候选只在 A4/A5 内选，`nested_v2_validation.py` 做外层 5 折。

### Weaknesses 与 Evidence

| 缺陷 | 类别与 Evidence | 结论 |
|---|---|---|
| Q 的人工/外部效度未完成 | D4；**E1：** `manual_validation_results.csv` 0 行，140 条双评审模板未填写 | $Q_A$ 只验证了实现一致性、权重/聚合稳健性、扩展一致性；没有 human validity。与 Loss 的增量关联仅 1M/60M 轻微改善，1B 略退。 |
| 指标语义仍有张力 | D1/D3；**E2：** 三个 DSIR 项与剩余 Q 的剔除自身相关约 -0.30，独特词比例约 -0.36；分域方向会翻转 | 不能为“修正相关”擅自翻转方向；需要人工标注区分真实冲突与域异质性。 |
| 跨规模绝对 Loss 失败 | D4/D5；**E1：** 60M/1B V2 RMSE 1.596/2.702，偏差 1.506/2.619；1B Spearman 0.877 低于 V1 0.888 | 二阶模型在小模型 1M 的优势不代表可跨规模直接给绝对 Loss。 |
| 领域交互和因果效应证据不足 | D2/D8；**E1/E3：** 16 个置换目标仅 13 个在 9 规格同号；二阶系数依赖 ALR 参照与 ε；配方是相关性实验 | 只能说模型条件下 1pp 局部预测差，不能宣称真实互补对。 |
| A12–A15 是估算标签 | D5；**E1/数据说明：** 10B/70B V2 RMSE 3.621/4.009，且表自身由小模型外推 | 不可作真实大规模验证。 |

**Q 证据分层。** Internal consistency = 转换复算、部分指标与 Q 的剔除自身相关；robustness = 权重/阈值/聚合扰动；extension consistency = 去重后同域均值相近；human/external validity = **Missing**；downstream association = 仅固定映射的探索性预测变化；独立训练效用 = **Missing**。

### Optimization hypotheses

- **H1，P1（High）。Current issue：** Q 缺人工效度。**Evidence：** 空结果表与已封存 140 文本盲评设计。**Hypothesis：** 若 Q 抓住人能辨识的质量，高低 Q 文本的双评审总体分应有稳定正向关联。**Proposed modification：** 先完成已设计双人盲评，不重选样本和权重。**Expected observable result：** Spearman 正向、95% 区间下界高于 0，并报告 ICC。**Falsification criterion：** 相关方向不稳定或评审一致性低，则只保留“工程代理”解释。**Risk：** 分层加抽冲突样本不能按总体比例外推。**Priority：P1。**
- **H2，P1（Low，暂缓）。** 只有 H1 提供独立标签后才预设检验惩罚是否改善人工判断；若只是内部排名变化或同一批标签反复调惩罚，拒绝升级。现有阈值和惩罚敏感性已经做过，不重复开发。
- **H3/H4/H5，已做。** 去重扩展、领域条件 bootstrap 和配比 ALR 支持审计均有结果。新增更复杂配比模型没有当前证据，不进入路线。

## 7 问题二专项审计

### Strengths

- **保留经典 $N$–$D$ 是正确决策。** `nd_model_scorecard.csv` 中 classic B1 组 CV RMSE 0.000151、BIC -20729；interaction/broken-N 未在两个外源和 BIC 上持续受益。BNSL 可作候选，不构成强制采用依据。
- **分清 B1 与 B2/B4/B5。** B1 八个 N 组共用 Pythia 体系，B3 是插值；B2 原始 RMSE 1.243、B4 0.293、B5 0.198。报告没有把少量目标源标签校准后的误差称零样本泛化。
- **Q×N 比 V1 加性 Q 有条件改善。** B6 按 N 留一 RMSE 0.08144→0.05235，B7 新点 0.05952→0.04213，增加 1 个参数；B8 1.3926→1.3418 仍失败。保留 Model C，限定“半合成条件机制”。
- **反常 B8 值得保留。** B6/B8 有 160 个相同 (N,D,Q) 组合对应不同 Loss，B8 的 Q 方向与 B6 反向。单一单调 Q 律无法同时准确解释，不能简单加一条更复杂残差曲线掩盖。

### Weaknesses 与 Evidence

| 缺陷 | 类别与 Evidence | 结论 |
|---|---|---|
| B1 误差虚假乐观于跨源任务 | D1/D4；**E1/E2：** 0.000151 vs B2 1.243；B1 Loss 四位小数且同训练族 | 不是逐行泄漏，而是同源验证覆盖不到来源差。 |
| B2 不只是截距偏移 | D3/D4；**E1：** 20% 目标源标签的仿射后期 holdout RMSE 0.6955；log-D 修正 0.3674，但在 B4/B5 不稳定 | 需要预先冻结形式并用新来源检验，不能在开发集里取最小。 |
| Model C 的 Q×N 外部真实性未知 | D1/D2；**E2：** B6/B7/B8 为半合成且 B8 方向相反 | 该交互可描述附件 B6 的生成机制，不代表真实训练 Q 效果。 |
| 配比桥接和质量尺度未识别 | D2/D6；**E5/E3：** P1 V2 模型嵌入 artifact；`lambda_p=1` 固定；Q_A 域常数所生 `Qmix=g(p)` 与 p 不独立；三条 QA→QB 均无锚 | (L(N,D,Q,p)) 是可计算的条件模型，而非四因素共同实验回归。 |
| 大规模外推真实检验不足 | D5；**E2/E5：** B9 是元数据、B10 是估算 Loss；V2 入口未重读 B9/B10，沿用 V1 在 $Q_B=1,p=p_0$ 时的 classic 诊断；70B/120B 等价倍数越过 B1 的 12B 上界 | V1 表只核对 V2 在参考情景退化后的经典项，未验证 Q×N 或配比的大规模效果；不把 B10 近完美误差称独立验证。 |

### Optimization hypotheses

- **H6，P2（High，有新标签才可验证）。Current issue：** B2 来源偏移和训练阶段残差。**Evidence：** raw RMSE 1.243、B2 后期仿射 0.6955、log-D 0.3674，但后者跨源不稳定。**Hypothesis：** 少量源标签能校准 Loss 口径，未必要更换 $N$–$D$ 指数。**Proposed modification：** 预先锁定一个低维校准式，使用真正未参与选择的来源/族及固定校准比例。**Expected observable result：** 至少两个目标来源的 holdout RMSE 和 bias 降，且新增参数成本合理。**Falsification criterion：** 只有已查看来源改善或新来源恶化，拒绝升级。**Risk：** 校准必需目标源标签，应用场景受限。**Priority：P1。**
- **H7，P2（High 溯源）。Current issue：** B8 质量方向与 B6 冲突。**Evidence：** 160 个同点不同 Loss，Q 分层差不是统一截距。**Hypothesis：** 数据源的 Q 语义/生成机制不同。**Proposed modification：** 核查半合成生成说明与同点残差，分机制报告；只有找到可验证共同变换才考虑联合式。**Expected observable result：** 来源标签能够解释 Q 方向和冲突结构。**Falsification criterion：** 新模型只是靠额外参数消掉训练残差、留组后无改善，拒绝。**Risk：** 生成规则可能不可见。**Priority：P1。**
- **H8/H9，跨问 High。** H8 的共享 $Q_A/Q_B$ 锚点需同文本或同配比异质量训练；无数据则维持 conservative/neutral/optimistic 三情景。H9 需先冻结 P1 V2 的跨规模逐配方预测，再设校准/留出分割验证 `lambda_p(N)`；只有独立留出 Loss 差误差或排序稳定提升才估系数。现有 V1 斜率不能直接冒充最终 V2 的精确估计。
- **H10 已部分完成。** P3 V2 已联合重采样 B1/B6 参数；跨源协方差仍不能由现有独立来源估出。

## 8 问题三专项审计

### Strengths

- **公式与单位一致。** 代码在十亿参数/token 单位使用 $C_{train}=6\times10^{18}N_BD_B$、$C_Q=10^9D_B[g(Q)-g(Q_0)]_+$、$C_{attn}=2\times10^{14}N_BD_BL_{ctx}$，解析临界 $L_{ctx}=6/(2\times10^{-4})=30000$。C7 支持 2048/4096/32768 敏感性；上下文是外生变量。
- **求解器有多层自检。** V1 `solve` 有 DE 与多初值 SLSQP、边界搜索；V2 `capped_solve` 对 $N,D$ 用硬边界，KKT/预算消元/预算绑定核验均通过。V2 结构转移点在 log 预算上二分，说明它是模型阈值而非真实统计 CI。
- **不确定性传播优于 V1。** 200 组参数、800 次最优求解保留 B6 内部相关；三类 Q 成本、Q0、上下文与 36 个稳健场景另列，不给这些人为场景赋自然概率。
- **支持域意识明确。** P1 观测配比 P1 的 $\Delta L=-0.5022$ 是支持内的模型预测；P3 loose simplex $-1.1951$ 标为 extrapolative。E0/E1/E2 并列阻止自由 Scaling Law 直接冒充可实施推荐。

### Weaknesses 与 Evidence

| 缺陷 | 类别与 Evidence | 结论 |
|---|---|---|
| E1 三倍边界是人为定义 | D5；**E3/E1：** 1e24 的 E1 (N=35.90B,D=899.68B) 双上界、只用 22.4% 预算；E2 (39.56B,3657B) 用满预算 | `moderate_3x` 是情景名称，不是被数据估计出的最优信任域。 |
| 高预算 regret=0 是边界几何 | D5/D8；**E1：** 36 场景均取同一角点 | 不能写成高预算风险消失。 |
| 质量饱和相对稳但成本非实测 | D1/D7；**E1：** 三类题设成本下 1e22/1e24 Q=1 常见；低预算 Q 差异大 | 只在给定成本类和 Q0 的条件下解释；没有真实数据质量处理算力曲线。 |
| 配比收益继承未识别 $\lambda_p$ | D2；**E1：** λ=1 收益 0.5022、λ=0 则并列；跨规模斜率只来自 P1 V1 探索 | 配比优化的排名可以稳定，收益幅度不能精确承诺。 |
| 极窄联合 bootstrap 不是现实 CI | D7；**E1/E3：** 条件 Loss 分位几乎重合但不含来源偏移、QA→QB、成本形状 | 与结构情景范围分开。 |

### Optimization hypotheses

- **H11，P3（High）。Current issue：** E1 3× 阈值主导 1e24。**Evidence：** E0/E1/E2 Loss 与预算使用率差异。**Hypothesis：** 若高预算配置并非由恰好三倍决定，在一组预设倍数上最优 (N,D,Q) 应渐进变化。**Proposed modification：** 做 1×、2×、3×、5×、自由上界剖面，只作为敏感性，不以新阈值替代实证。**Expected observable result：** 推荐范围及 Loss 曲线平滑。**Falsification criterion：** 小改阈值就改变主结论，则拒绝给单点推荐。**Risk：** 阈值本身仍无外部真值。**Priority：P1。**
- **H12/H13/H14/H15 已在 V2 处理或部分处理。** 零后悔退化已披露；λ_p 情景与三成本敏感性已有；联合参数 bootstrap 已做。剩余需求是上游的 H8/H9 数据，而非继续调优化器。

**1e24 可信度判定：** 数值最优性 Strong；在 B1 训练支持内的条件建议 Acceptable；E1 三倍配置作为现实资源建议 Weak；E2 自由配置 Level C。切勿因 KKT 极准而提高外推证据等级。

## 9 问题四专项审计

### Strengths

- **C8 确实进入代码。** 1,958 个 JSON、1,863 个目录中成功解析 1,860 个，40 叶任务、74,344 个模型-任务分数；任务百分位与六维均值 Pearson 0.952，弱项指标 0.918。损坏文件记录而未静默丢弃。
- **实体、开放权重和许可分类谨慎。** C1 4,576 行中仅 46 行严格实体匹配，主开放权重独立模型 45 个（pretrained 32、chat/finetuned 10、其他 3）；W2 明确许可标签仅 27 个。避免把 C2 的继承基础模型元数据自动当直接匹配。
- **桥接有家族分组检验。** C6 75 行，高可比 7、中可比 68；主单调有界 logistic 家族组外 RMSE 11.864，误差公开。训练集拟合、CV、可比层级都保存。
- **失败诊断真实保留。** 结构模型组外 RMSE 7.103，略差于 compute-only 7.052；模型内变化 +6.84 与实测 -2.80 方向冲突；3 月两折回测 RMSE 约 7.96，6 月唯一折误差 18.80，12/24 月 0 折。

### Weaknesses 与 Evidence

| 缺陷 | 类别与 Evidence | 结论 |
|---|---|---|
| 历史长度和严格前沿太小 | D1/D4；**E1：** 仅 2024Q2–2025Q1 四季度，季度 (n=22,16,5,2)；C2 继承宽口径趋势相反 | 严格 n=2 的 p95 几乎退化成较大单点，不能稳定识别技术贡献。 |
| 结构模型可能过度解释 | D2/D3；**E1：** 连续能力回归 n=32、13 组织、3 参数（含截距），约 10.7 样本/参；`nd_time` n=16、4 参数，只有 4 样本/参；组外折 RMSE 结构 4.35–10.75 | 不是分类任务，无“events per variable”定义；按样本/参数和组织/参数更适合。增加变量会恶化可识别性。 |
| 技术时间项不是纯技术 | D2/D8；**E1/E3：** 结构模型不优于 compute-only，且分解模型与实测方向相反 | 时间残差混合样本选择、榜单提交、后训练、评测适配；31.5%/68.5% 只可称模型内探索性分解。 |
| 桥接精度不足以支撑细点预测 | D1/D4；**E1：** 主 CV RMSE 11.864；高可比仅7；同 Loss=2 时不同权重模型预测差明显 | 20.94 一类数字的两位小数是计算精度，不是知识精度。 |
| 未来区间过宽且无长期回测 | D4/D7；**E1：** 中度放缓 24 月 95% 条件区间 [22.14,99.15]；0 个同期限折 | 区间尚未校准覆盖率，情景倍率也不是概率分布。 |
| 机制 E1 被上界钳制 | D5；**E1/E3：** P4 未来预算 5.64e24–1.81e26；E1 在 (N=35.897B,D=899.679B,Q=1) 后 12/24 月均 20.94 | 这是 E1 资源上限的结果，不是能力停滞实证；必须并列 E0/E1/E2 与数据驱动。 |
| 预测基线时间锚不同 | D6/D7；**E5：** `forecast()` 的能力锚取 C1 最后观测季 2025Q1，算力锚取 C4 最后完整季 2024Q4 | 代码有注释且非隐蔽错误，但论文要披露并做共同季度敏感性。 |

### Optimization hypotheses

- **H16/H17，P4（High）。Current issue：** W1 严格匹配后末季 2 个模型。**Evidence：** W1 -2.80 与 C2 继承宽口径 +10.01。**Hypothesis：** 前沿方向主要受样本选择和 n 太小影响。**Proposed modification：** 预设最低季度样本数，比较滚动窗 p90/p95/Top3，W1 仍为可信主口径，W2/C2 只作敏感性。**Expected observable result：** 合理前沿定义下方向和 3 月回测稳定。**Falsification criterion：** 方向持续随口径翻转，则放弃历史百分比分解主结论。**Risk：** 滚动窗平滑会牺牲季度分辨率，宽口径可能误匹配。**Priority：P1。**
- **H18，P4（High）。Current issue：** 模型内 +6.84 与观测 -2.80 冲突。**Evidence：** `scale_technology_contributions.csv` 和 compute-only/structural 组外误差。**Hypothesis：** 当前时间项更多是选择/口径混合而非可识别技术进步。**Proposed modification：** 先把 31.5%/68.5% 降为模型内敏感性；只在 H17 给出稳定前沿、时间项改善组外预测且方向一致后恢复份额结论。**Expected observable result：** 模型隐含与实测同向。**Falsification criterion：** 继续反向则只讨论无法识别。**Risk：** 无法解决短历史的根本限制。**Priority：P1。**
- **H19，P4（High）。Current issue：** Loss→Benchmark RMSE 11.864。**Evidence：** C6 高可比 7、混合来源 68；isotonic 组外均方误差略低但有平坦外推。**Hypothesis：** 对单项任务和可比性分层可减少系统误差。**Proposed modification：** 锁定家族 CV，比较少参数单调映射与六项分任务映射，不因训练拟合更好就换主模型。**Expected observable result：** 各折误差下降超出折间波动且高可比方向一致。**Falsification criterion：** 只有训练误差降低或外推不稳定，保留有界 logistic。**Risk：** 每任务样本更稀。**Priority：P1。**
- **H20/H21，P4（High）。** 把预测不确定性拆成系数、历史残差、算力路径、技术情景、桥接误差及 E0/E1/E2 边界；以可复核的分量贡献替代未经校准的“95%可信未来范围”。P4 已算出 E0/E1/E2，不必重建模型；正文应并列。若分量无法稳定估计，就只给情景包络，拒绝精确 24 月点承诺。
- **H24，P4（Medium）。** 把能力与算力锚都放在 2024Q4 做敏感性；若 12 月结果变化超过当前模型误差尺度，则标注锚点敏感，不能只展示 2025Q1 能力/2024Q4 算力混合锚。

## 10 四问接口与版本审计

| 箭头 | 实际代码路径 | 判定 |
|---|---|---|
| P1 $Q_A$ → P2 | P2 V1 读取 `domain_quality_scores.csv` 到 `problem1_quality_proxy_import.csv`；V2 真正拟合 Q 项用 B6 `Q_score` | **只有描述性读取，数值桥接未识别。** |
| P1 π配比 → P2 | V1 artifact 先加载 P1 `mixture_v2.joblib`，P2 V2 从该 artifact 拷贝嵌入；本次独立比较 `pcols` 和 Ridge 系数逐项相等 | **实际传入最终 P1 V2**；同时 V2 的 `p_scale_transfer_from_problem1.csv` 部分参考旧 V1 跨规模预测。 |
| P2 V2 → P3 V2 | `problem3_v2/run_v2.py` 直接加载 `recommended_model.joblib` 且断言 `classic/quality_model` | **实际传入最终 Model C。** |
| P3 V2 → P4 | `problem4/mechanism.py` 导入 V2 `solve/capped_solve`，读取 V2 配比支持表，在未来预算重算 | **方法实际传入；最优表并未直接读取。** P4 验证器只核对情景数和接口存在，未比对所有参数哈希。 |
| P2/P3 Loss → P4 Benchmark | P4 读取 C6 桥接，再把重算 Loss 输入主 logistic | **数值链存在，统计桥接弱。** |

**H22（High，接口最小修改）。Current issue：** 旧 JSON 并存且 `verify_problem4.py` 第 40 行只核对 V2 接口存在及三个 regime。**Evidence：E5 代码检查。Hypothesis：** 显式 artifact 参数和 SHA-256 对照能排除隐藏版本漂移。**Proposed modification：** 下一轮先加只读接口清单：P1 配比系数、P2 八参数与 `lambda_p`、P3 成本/Q0/C7、P4 重算输入逐项相等。**Expected observable result：** 现有链全部相等；不等则定位为 P0。**Falsification criterion：** 任一数值或模型哈希与当前推荐版本不同，暂停受影响结论。**Risk：** 旧实验结果需保留历史 provenance。**Priority：P1；若发现不一致立刻升 P0。**

**复现顺序。** 现有脚本不是一个干净目录可直接只跑 P4 的闭包：P1 V1 质量/配比流程 → P1 V2 → P2 V1 → P2 V2 → P3 V1（写 P2 V2 接口 JSON）→ P3 V2（读 P3 V1 表）→ P4。P4 README 的“一条命令”指其先决产物已存在。H23 应先写依赖 DAG 和只读产物校验，随后才考虑统一执行入口。

## 11 数据泄漏、训练/开发/测试状态

1. **未证实逐行泄漏。** P1 V2 候选由 A4/A5 内部 CV 选择；A6–A15 未进入该轮参数拟合。P2 $N$–$D$ 只用 B1，Q 参数只用 B6；B8 只诊断。P4 C6 使用家族 GroupKFold，历史回测不把目标季数据放进训练。
2. **不能称全新盲测。** P1 V1 曾看过 A6–A15；P1 V2 的验证因此是“锁定模型的既定测试同口径比较”。P2 B2/B4/B5/B7 已用于多候选研究判断，20% 校准形式也被查看；其剩余 80% 是条件 holdout，非全新独立确认。
3. **估算值不做实测验证。** A12–A15、B10 的“好结果”不能提升真实外推等级。
4. **Q 人工盲评不可事后调分。** 一旦 140 条评分被查看，再用这批数据反复选权重/惩罚会使它丧失外部验证地位。需要预先冻结比较方案或新留出评分。

## 12 公式、单位、数值与 P0 排查

- P2 Model C 在代码 `predict_q('quality_model')` 为 $E+AN^{-\alpha}+BD^{-\beta}+G(1-Q_B)^\kappa N^{-\eta_N}$；`predict_recommended` 再加 $\lambda_p\Delta_p$。P3 `Model.loss` 采用相同八参数。未发现这条公式链与实际代码不一致。
- P3 的十亿单位换算如第 8 节，与赛题 $6ND, D[g-g_0]_+, \eta NDL_{ctx}$ 一致；V2 消元、预算、KKT 检验支持数值实现。
- P4 结构模型设计矩阵含截距、log10 compute、时间；Shapley 两贡献严格相加到**模型隐含**变化，观测前沿不同向是统计适用性问题，不是加法公式 bug。
- 本轮**未发现确认的 P0 代码 bug、单位错误、公式/代码不一致**。接口只做存在性检查和 W1/C4 时间锚不同，是值得验证的风险，不能无证据宣布成实际数值错误。
- 根目录赛题/数据说明把 RegMix 会议写为 ICML 2024；正式论文为 [ICLR 2025](https://proceedings.iclr.cc/paper_files/paper/2025/file/5f67d864aae6115374fed7beddd119e0-Paper-Conference.pdf)。这是来源元数据/论文引用需统一的**文档错误**，不影响当前求解数值。

## 13 外推与不确定性传播

P1 的 1M 训练支持之外，60M/1B 主要可读“排序趋势”，10B/70B 是估算标签。P2 的 B1 上界约 12B，70B/120B 等价参数和 B10 Loss 比对只作敏感性。P3 的 1e24 E2 $N,D$ 都超出 B1 支持；E1 3× 虽降低越界程度，却不是从数据估出的可信域。P4 预测算力已到 $5.64\times10^{24}$–$1.81\times10^{26}$ FLOPs，超过 P3 主展示的 1e24，故 E1 钳制与 E2 强外推同时存在。各层不得把参数 bootstrap 的极窄区间、成本情景包络、桥接 CV 误差直接合成为一个“95% 现实置信区间”。

## 14 证据等级

[`evidence_level_audit.csv`](evidence_level_audit.csv) 逐项列 A/B/C/D。**Level A** 主要是 P1 全量处理与 1M 配比预测改进、P2 B1 同族经典律、P3 给定模型成本下的数值求解、P4 C8 逐任务解析。**Level B** 包括 Q 扩展一致性、B6/B7 半合成 Q×N、P3 条件结构转移、C6 有误差的桥接。**Level C** 包括 $\lambda_p=1$ 收益、1e24 E2、P4 历史份额与 12/24 月具体数值。**Level D** 包括“Q_A 已证实真实训练价值”“统一跨源 Q 因果规律”“零 regret 表示风险为零”。

## 15 可疑“过好”结果

见 [`suspiciously_good_results.md`](suspiciously_good_results.md)。重点是 B1 0.000151、B3 插值近完美、B10 估算标签近完美、P3 1e22 极窄 bootstrap 与高预算零 regret。此次代码/数据追踪分别指向同源近确定性、插值、共享生成规律、条件重抽样与硬上界角点；没有发现因此必然存在数据泄漏的证据。P4 C8 与六维高相关也属同一榜单的构念一致性，不能当独立外部验证。

## 16 有价值的失败结果

| 失败 | 应保留的知识 | 不建议的做法 |
|---|---|---|
| B2 raw RMSE 1.243 | 同族 Scaling Law 无法自动跨 Loss 口径；源校准需要目标标签 | 调 B1 指数使 B2 误差变小后称零样本泛化 |
| B8 同点冲突与 Q 反向 | 单一半合成 Q 机制不兼容 | 用高阶函数混合拟合并删除方向异常点 |
| P1 60M/1B 数值偏移 | 小模型配比模型的绝对 Loss 可迁移性有限 | 只展示 Spearman 隐去 RMSE/bias |
| P4 桥接 RMSE 11.86 | Loss 和多维 Benchmark 跨模型并不等价 | 把 20.94 保留两位小数并称能力实测预测 |
| P4 模型 +6.84 vs 实测 -2.80 | 严格前沿选择/时间项识别不稳 | 只报告 31.5%/68.5% 的漂亮比例 |
| P4 24月区间近 22–99 | 短历史不足以精确预测远期前沿 | 继续增加时间多项式或自信点预测 |

## 17 正式文献依据

逐篇 Title、Authors、Year、Venue、URL/DOI、与具体缺陷的关系和不可迁移部分，见 [`evidence_literature_review.md`](evidence_literature_review.md)。文献支持保留经典律、用对数比处理配比、以受控实验检验质量与配比迁移、对短时间序列作时间外验证；文献**不能**为本项目未识别的 $Q_A\to Q_B$、λ_p 或 3× 边界提供现成数值。

## 18 优化候选与统一验收流程

所有 H1–H24 的 Current issue、Evidence、Hypothesis、Proposed modification、Expected observable result、Falsification criterion、Risk、Priority 和下游影响已写入 [`optimization_hypotheses.csv`](optimization_hypotheses.csv)。其中 H3/H4/H5/H10/H12/H13/H14/H15 是已解决或仅需条件表述，不应作为“下一轮新工作”重复开发。

```text
发现缺陷与证据 → 预注册最小改动假设 → 冻结数据划分与评价指标
→ 只在开发集实施 → 对预留集/独立来源验证 → 按接受与证伪门槛决定
→ 不通过则保留当前模型及失败结果
```

升级至少要有两项：理论/数据依据、验证改善、复杂度合理。只降低训练 RMSE 不能升级。收益/成本/过拟合风险见 [`optimization_priority_matrix.csv`](optimization_priority_matrix.csv)。

## 19 P0/P1/P2/P3 优先级

- **P0：本轮未确认需修改的 P0 数值 bug。** 若 H22 发现 P2/P3/P4 实际模型参数、单位或版本不一致，立即升 P0，冻结受影响输出；正式提交前的 AI 使用披露与四问合稿也是提交门槛，但不是当前模型数值 bug。
- **P1：** H1、H6（仅当有真正新源标签）、H7、H8（需共同锚点）、H9、H11、H16–H22、H23。首要是证据等级和链条一致性，而非扩大候选模型库。
- **P2：** H24 时间锚敏感性、更多可解释的区间分解、完整运行 DAG 和论文图表精简。
- **P3：** H2 在无人工标签前重调冲突惩罚；再加三阶/神经网络配比；强行拟合 λ_p；为零 regret 继续调求解器；给三种题设成本主观概率。

## 20 Top 5 Highest-Value Improvements

仅列五项，按可阻断下游误读的价值排序。下一轮实施仍需遵守各 H 的接受/拒绝标准。

| 顺序 | 工作 | 为什么重要；影响 | 证据与预计文件 | 成功解决什么；失败意味着什么 |
|---|---|---|---|---|
| 1 | **冻结并核验跨问数值接口（H22/H23）** | 防止 P1→P4 在版本并存下无声漂移；影响四问 | `problem2_v2/run_v2.py`、`problem3/load_inputs.py`、`problem3_v2/run_v2.py`、`problem4/mechanism.py`、验证脚本；现有验证器只看存在性 | 参数/哈希一致则可信地引用当前版本；不一致则升 P0 并重算受影响结果 |
| 2 | **完成 Q 双人盲评，同时明确 QA→QB 只能情景连接（H1/H8）** | 最大上游质量风险，影响 P1–P4 | `problem1/analyze_manual_quality.py`、140 条模板、`QA_QB_scenarios.csv`；人工结果当前 0 行 | 人工效度通过仅提升 QA 的解释；若不通过则不得把 Q 称真实质量。无共同锚点时仍不得标定 QB |
| 3 | **对 P4 严格前沿设最低样本规则并重审贡献份额（H16–H18）** | 最大下游科学结论风险，影响 P4 主答案 | `problem4/history_forecast.py`、`quarterly_frontier.csv`、`scale_technology_contributions.csv`；末季 n2 且模型/实测反向 | 方向稳定才讨论份额；仍反向则报告无法稳健分离，并保留模型内情景 |
| 4 | **以最终 P1 V2 预测重新验证配比跨规模幅度（H9）** | λ_p 当前是默认值，影响 P2–P4 配比收益 | `problem1/problem1_v2.py`、`problem2_v2/outputs/tables/p_scale_transfer_from_problem1.csv`、`problem3_v2/mixture_evidence.py` | 独立留出改善才估 λ_p；失败则维持多情景、不报精确收益 |
| 5 | **拆解 P4 桥接与机制预测的不确定性并并列 E0/E1/E2（H19–H21）** | 20.94 与数据预测差巨大，影响 P4 最终解释 | `problem4/bridge_model.py`、`mechanism.py`、`loss_benchmark_bridge_cv.csv`、`mechanistic_forecast.csv` | 若组外桥接真改善则升级；若没有，保留 logistic 及误差，报告边界导致的差异 |

如果新增数据不可得，H6/H8 的“估参”步骤不能假装已经完成；应保留原失败与情景。P3 的阈值剖面 H11 仍值得做，但其科学增益低于先确认前四条链的证据。

## 21 Low-Value / Unjustified Optimizations

- 盲目用神经网络取代 P1 二阶 ALR 或 P2 classic；现有样本与外部误差没有支持更复杂结构。
- 无限增加配比高阶交互、把局部预测差叫因果互补；13/16 符号稳定仍不足以识别交互机制。
- 为压低 B2/B8 RMSE 把外部集反复调成训练集；将失去跨源失败这一重要边界。
- 用 B10 估算 Loss 或 A12–A15 估算 Loss 证明大规模实测外推。
- 用更复杂时间趋势给 P4 24 月窄区间；32 个主回归样本、四季度前沿和 0 个长期回测不支持。
- 擅自把 DSIR/独特词比例反向或重新定 Q 权重；域内方向异质且没有人工标签。
- 再次优化 P3 KKT 或追求比 0 更低 regret；当前瓶颈是输入证据与边界，不是优化器精度。

## 22 最终推荐路线与提交状态

**若只做 1 项：** 先做 Top 5 第 1 项，即锁定四问最终版本、artifact 哈希和参数接口。这是后续所有结论可复核的前提，工作量小，也能迅速排除真正的 P0 错接。

**若可做 3 项：** 依次完成接口冻结、问题一预设双人盲评、问题四最低样本规则与贡献分解复核。前两项稳住上游定义，第三项防止论文最醒目的百分比结论超过证据。

**当前能否提交：** 作为研究代码与分问题报告，已有相当完整的可审计结果；作为竞赛最终提交，**当前工作区尚不宜直接提交**。仅有问题一 Word 正文，未见四问合并论文；该 Word 没有 AI 使用披露，而赛题要求论文末尾据实披露。P4 份额和远期点预测必须在合稿中明确标为探索性；P1 $Q_A$ 与 P2 $Q_B$ 必须保持两个量尺的表述。完成审计建议后，提升主要体现为**版本可追踪、外部效度、条件结论与真正证据边界**，而非保证每个 RMSE 更低。
